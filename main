#!/usr/bin/env python3
"""
Reverse Guess the Number (Computer guesses).

How to play:
- You think of a number between 1 and 100.
- The computer will try to guess it.
- After each guess, you provide feedback: higher, lower, or correct.
- The computer uses binary search logic to find your number efficiently.
"""

def main():
    print("Think of a number between 1 and 100, and I will try to guess it!")
    input("Press Enter when ready...")

    low, high = 1, 100
    attempts = 0

    while low <= high:
        guess = (low + high) // 2
        attempts += 1
        print(f"My guess is {guess}.")
        feedback = input("Is it correct, higher, or lower? (c/h/l): ").strip().lower()

        if feedback == 'c':
            print(f"Yay! I guessed your number {guess} in {attempts} attempts!")
            break
        elif feedback == 'h':
            low = guess + 1
        elif feedback == 'l':
            high = guess - 1
        else:
            print("Invalid input. Please enter 'c' for correct, 'h' for higher, or 'l' for lower.")

    else:
        print("Hmm, something went wrong. Did you give consistent hints?")

if __name__ == "__main__":
    try:
        main()
    except (KeyboardInterrupt, EOFError):
        print("\nGoodbye!")
